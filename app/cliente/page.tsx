import { ClientDashboard } from "./ClientDashboard";

export const metadata = {
  title: "Minha Agenda | BarberPro",
  description: "Dashboard do cliente BarberPro"
};

export default function ClientePage() {
  return <ClientDashboard />;
}
